{
    "extends": "./tsconfig.json",
    "include": [
        "src/**/*.ts",
        "scripts/**/*.ts",
        "__tests__/**/*.ts",
        "*.ts",
        "types/**/*.d.ts"
    ],
    "exclude": ["node_modules", "dist", "dist-tools", "coverage", "drafts"]
}
